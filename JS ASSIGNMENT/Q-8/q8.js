let string = "  Myname  "

console.log(`length of string including white spaces is ${string.length}`);

console.log(`\nUse trim function to get actual length without whitespaces`);

console.log(`\nActual length is ${string.trim().length}`);